# robots.txt for aervsolutions.com
#
# Opts the site out of AI *training* crawlers. Normal search engines and AI
# search/answer agents (which fetch pages to answer a user's question and
# link back, e.g. OAI-SearchBot, ChatGPT-User, PerplexityBot,
# Claude-SearchBot) stay allowed, so the business stays findable.
#
# This file is advisory: only well-behaved crawlers obey it. It doesn't
# protect the chat API, which is protected by its own daily quota and the
# on/off switch (see infrastructure/README.md → Chatbot).

User-agent: GPTBot
Disallow: /

User-agent: ClaudeBot
Disallow: /

User-agent: CCBot
Disallow: /

User-agent: Google-Extended
Disallow: /

User-agent: Applebot-Extended
Disallow: /

User-agent: Meta-ExternalAgent
Disallow: /

User-agent: Bytespider
Disallow: /

User-agent: *
Allow: /
